import { type QueryOptions, type TripType } from "./types.js";
export interface SelectedFlightInfo {
    originAirport: string;
    date: string;
    destinationAirport: string;
    airlineCode: string;
    flightNumber: string;
}
/** Build the f.req payload for GetShoppingResults. */
export declare function buildPayload(segments: unknown[][], tripType?: TripType, options?: QueryOptions): string;
/**
 * Build payload for a one-way query.
 */
export declare function buildOneWayPayload(origin: string, destination: string, date: string, options?: QueryOptions): string;
/**
 * Build payload for a round-trip query.
 */
export declare function buildRoundTripPayload(origin: string, destination: string, departureDate: string, returnDate: string, options?: QueryOptions): string;
export interface MultiCityLeg {
    origin: string;
    destination: string;
    date: string;
}
/**
 * Build payload for a multi-city query.
 */
export declare function buildMultiCityPayload(legs: MultiCityLeg[], options?: QueryOptions): string;
/**
 * Build payload for step 2+ of a multi-city query.
 * Includes the booking token and selected flight info from previous steps.
 */
export declare function buildMultiCityStepPayload(legs: MultiCityLeg[], bookingToken: string, selectedFlights: SelectedFlightInfo[], options?: QueryOptions): string;
